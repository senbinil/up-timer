<div class="w-full max-w-md space-y-6">
  <div class="space-y-2">
    <p class="text-xs font-semibold uppercase tracking-[0.05em] text-slate-400 font-mono">Password</p>
    <h1 class="text-[32px] font-semibold leading-10 tracking-[-0.01em] text-navy font-[Hanken_Grotesk]">Forgot Password</h1>
    <p class="text-body-lg text-slate-400">Enter your email to receive a password reset link.</p>
  </div>

  <div class="card">
    <%= form_with url: rodauth.reset_password_request_path, method: :post, data: { turbo: false }, class: "space-y-8" do |form| %>
      <% if params[rodauth.login_param] && !rodauth.field_error(rodauth.login_param) %>
        <%= form.hidden_field rodauth.login_param, value: params[rodauth.login_param] %>
      <% else %>
        <div class="space-y-1.5">
          <%= form.label "login", "Email", class: "input-label block" %>
          <div class="relative">
            <span class="absolute inset-y-0 left-0 flex items-center pl-3 text-slate-400">
              <%= lucide_icon "mail", size: 16 %>
            </span>
            <%= form.email_field rodauth.login_param, value: params[rodauth.login_param], id: "login", autocomplete: "email", required: true, class: "input-field pl-10 #{rodauth.field_error(rodauth.login_param) ? "!border-error" : ""}", aria: ({ invalid: true, describedby: "login_error_message" } if rodauth.field_error(rodauth.login_param)) %>
          </div>
          <%= content_tag(:p, rodauth.field_error(rodauth.login_param), class: "text-xs text-error font-mono mt-1", id: "login_error_message") if rodauth.field_error(rodauth.login_param) %>
        </div>
      <% end %>

      <%= form.button rodauth.reset_password_request_button, class: "btn btn-primary w-full justify-center" %>
    <% end %>
  </div>

  <p class="text-center text-sm text-slate-400">
    <%= link_to "Back to Log In", rodauth.login_path, class: "text-primary-container hover:underline font-medium" %>
  </p>
</div>
